<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="gerard.example.munchkinhelper">

    <uses-feature android:glEsVersion="0x00030000" android:required="true" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_new_launcher_round"

            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name="gerard.example.munchkinhelper.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.GameActivity"
                android:theme="@style/AppTheme.NoActionBar"
                android:configChanges="screenSize"/>
        <activity android:name=".ui.activity.create.AddingPlayersActivity"/>
        <activity android:name=".ui.activity.load.LoadGameActivity"/>
    </application>

</manifest>